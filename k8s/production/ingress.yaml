apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-johnpofo-monapp3-backend-ingress
  namespace: prod-johnpofo-monapp3-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - mydomain.api.flapi.org
      secretName: prod-johnpofo-monapp3-backend-tls
  rules:
    - host: mydomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-johnpofo-monapp3-backend-service
                port:
                  number: 3333
