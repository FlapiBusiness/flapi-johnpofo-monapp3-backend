apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-johnpofo-monapp3-backend-ingress
  namespace: staging-johnpofo-monapp3-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.mydomain.api.flapi.org
      secretName: staging-johnpofo-monapp3-backend-tls
  rules:
    - host: staging.mydomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-johnpofo-monapp3-backend-service
                port:
                  number: 3333
